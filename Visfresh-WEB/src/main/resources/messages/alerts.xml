<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<!-- 
	common shipment issue place holders:
	${date} alert issue date include day and year
	${time} the time in scope of day.
	${device} device IMEI
	${devicesn} device serial number
	${tripCount} trip count for given device of shipment.

	for alerts:
	${type} alert type
		
	for temperature alerts:
	${temperature}
	${period}
	
	for arrivals:
	${mettersForArrival} the number of meters for arrival
	 -->
	<!-- Cold -->
	<entry key="Cold">Cold Alert. Tracker ${devicesn}(${tripCount}) went below ${temperature} for ${period}min</entry>
	<!-- Hot -->
	<!-- Example: Too hot alert - tracker 098324(12) went above 20,0°C degrees for 10 min -->
	<entry key="Hot">Hot Alert. Tracker ${devicesn}(${tripCount}) went above ${temperature} for ${period}min</entry>
	<!-- CriticalCold -->
	<entry key="CriticalCold">Critical Cold Alert. Tracker ${devicesn}(${tripCount}) went below ${temperature} for ${period}min</entry>
	<!-- CriticalHot -->
	<entry key="CriticalHot">Hot Alert. Tracker ${devicesn}(${tripCount}) went above ${temperature} for ${period}min</entry>

	<!-- Cumulative -->
	<entry key="Cold.cumulative">Cold Alert. Tracker ${devicesn}(${tripCount}) went below ${temperature} for ${period}min in total</entry>
	<!-- Hot -->
	<!-- Example: Too hot alert - tracker 098324(12) went above 20,0°C degrees for 10 min -->
	<entry key="Hot.cumulative">Hot Alert. Tracker ${devicesn}(${tripCount}) went above ${temperature} for ${period}min in total</entry>
	<!-- CriticalCold -->
	<entry key="CriticalCold.cumulative">Critical Cold Alert. Tracker ${devicesn}(${tripCount}) went below ${temperature} for ${period}min in total</entry>
	<!-- CriticalHot -->
	<entry key="CriticalHot.cumulative">Hot Alert. Tracker ${devicesn}(${tripCount}) went above ${temperature} for ${period}min in total</entry>

	<!-- LightOn -->
	<entry key="LightOn">Light Alert. Entered bright environment at ${time}</entry>
	<!-- LightOff -->
	<entry key="LightOff">Light Off Alert. Entered dark environment at ${time}</entry>
	<!-- MovementStart -->
	<entry key="MovementStart">Movement Alert. Sudden movement occurred at ${time}</entry>
	<!-- Battery -->
	<entry key="Battery">Battery Alert. Low battery at ${time}</entry>
	<!-- Arrival -->
	<entry key="Arrival">Tracker ${devicesn}(${tripCount}) is in ${mettersForArrival} meters for arrival</entry>
</properties>
