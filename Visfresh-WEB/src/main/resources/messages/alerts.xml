<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<!-- 
	supported place holders:
	${date} alert issue date include day and year
	${time} the time in scope of day.
	${type} alert type
	${device} device IMEI
	${devicesn} device serial number
	${tripCount} trip count for given device of shipment.
	
	for temperature alerts:
	${temperature}
	${period}
	 -->
	<!-- Cold -->
	<entry key="Cold">Too cold alert - tracker ${devicesn}(${tripCount}) went below ${temperature} degrees for ${period} min</entry>
	<!-- Hot -->
	<!-- Example: Too hot alert - tracker 098324(12) went above 20,0°C degrees for 10 min -->
	<entry key="Hot">Too hot alert - tracker ${devicesn}(${tripCount}) went above ${temperature} degrees for ${period} min</entry>
	<!-- CriticalCold -->
	<entry key="CriticalCold">Critical cold alert - tracker ${devicesn}(${tripCount}) went below ${temperature} degrees for ${period} min</entry>
	<!-- CriticalHot -->
	<entry key="CriticalHot">Too hot alert - tracker ${devicesn}(${tripCount}) went above ${temperature} degrees for ${period} min</entry>
	<!-- LightOn -->
	<entry key="LightOn">Light on at ${time}</entry>
	<!-- LightOff -->
	<entry key="LightOff">Light off at ${time}</entry>
	<!-- MovementStart -->
	<entry key="MovementStart">Movement starts at ${time}</entry>
	<!-- Battery -->
	<entry key="Battery">Battery low at ${time}</entry>
</properties>
